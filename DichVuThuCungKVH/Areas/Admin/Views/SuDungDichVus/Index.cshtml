@model IEnumerable<DichVuThuCungKVH.Model.SuDungDichVu>

@{
    ViewBag.Title = "Quản Lý Lượt Sử Dụng Dịch Vụ";
    Layout = "~/Areas/Admin/Views/Shared/_LayoutAdmin.cshtml";
}

<div class="container-fluid px-4">
    <div class="card shadow mb-4">
        <div class="card-header py-3 d-flex justify-content-between align-items-center">
            <h6 class="m-0 font-weight-bold text-primary">Danh Sách Lượt Sử Dụng Dịch Vụ</h6>
            <a href="@Url.Action("Create")" class="btn btn-primary">
                <i class="fas fa-plus"></i> Thêm Mới
            </a>
        </div>
        <div class="card-body">
            <div class="table-responsive">
                <table class="table table-bordered table-hover" id="dataTable" width="100%" cellspacing="0">
                    <thead class="table-light">
                        <tr>
                            <th>Ngày Sử Dụng</th>
                            <th>Ghi Chú</th>
                            <th>Khách Hàng</th>
                            <th>Thao Tác</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model)
                        {
                            <tr>
                                <td>
                                    <i class="fas fa-calendar text-primary me-2"></i>
                                    @Html.DisplayFor(modelItem => item.Ngay)
                                </td>
                                <td>
                                    <i class="fas fa-sticky-note text-primary me-2"></i>
                                    @(string.IsNullOrEmpty(item.GhiChu) ? "Không có" : item.GhiChu)
                                </td>
                                <td>
                                    <i class="fas fa-user text-primary me-2"></i>
                                    @Html.DisplayFor(modelItem => item.KhachHang.TenKH)
                                </td>
                                <td>
                                    <div class="dropdown">
                                        <button class="btn btn-secondary dropdown-toggle" type="button" id="dropdownMenuButton_@item.MaSDDV" data-bs-toggle="dropdown" aria-expanded="false">
                                            <i class="fas fa-cog"></i> Thao tác
                                        </button>
                                        <ul class="dropdown-menu" aria-labelledby="dropdownMenuButton_@item.MaSDDV">
                                            <li>
                                                <a class="dropdown-item" href="@Url.Action("Details", new { id = item.MaSDDV })">
                                                    <i class="fas fa-eye text-info"></i> Xem chi tiết
                                                </a>
                                            </li>
                                            <li>
                                                <a class="dropdown-item" href="@Url.Action("Edit", new { id = item.MaSDDV })">
                                                    <i class="fas fa-edit text-primary"></i> Sửa thông tin
                                                </a>
                                            </li>
                                            <li>
                                                <a class="dropdown-item" href="@Url.Action("Delete", new { id = item.MaSDDV })">
                                                    <i class="fas fa-trash text-danger"></i> Xóa
                                                </a>
                                            </li>
                                        </ul>
                                    </div>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <script>
        $(document).ready(function () {
            $('#dataTable').DataTable({
                language: {
                    url: '//cdn.datatables.net/plug-ins/1.13.7/i18n/vi.json'
                },
                order: [[0, 'desc']],
                columnDefs: [
                    { orderable: false, targets: 3 }
                ]
            });

            $('[title]').tooltip({
                placement: 'top',
                trigger: 'hover'
            });
        });
    </script>
}
